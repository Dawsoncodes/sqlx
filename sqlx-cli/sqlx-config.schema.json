{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
        "env_path": {
            "type": "string",
            "description": "Path to the .env file, this ignores the --database-url option when the command is run. If not provided, the .env file in the current directory is used if it exists, otherwise it gets the database url from the command line option \"--database-url.\"",
            "default": ".env",
            "examples": ["../.env", "../myapp/.env"]
        }
    }
}
